version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env.example
    networks:
      - app-network
  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    networks:
      - app-network
  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend
    networks:
      - app-network
networks:
  app-network:
    driver: bridge
